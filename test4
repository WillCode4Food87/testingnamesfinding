/* UxRatingPrompt.razor.css (scoped CSS) */

.ux-rating-dock {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 12px;
  z-index: 1050; /* above most content */
  display: none;
}

.ux-rating-dock.is-open {
  display: block;
}

.ux-rating-card {
  position: relative;
  max-width: 640px;
  margin: 0 auto;
  background: #fff;
  border: 1px solid rgba(0,0,0,.1);
  border-radius: 16px;
  padding: 16px;
  box-shadow: 0 .75rem 1.5rem rgba(0,0,0,.15);
}

.ux-close {
  position: absolute;
  top: 12px;
  right: 12px;
}

.ux-title {
  font-weight: 600;
  font-size: 1.05rem;
  margin-right: 36px; /* leave space for X */
}

.ux-stars {
  margin-top: 12px;
  display: flex;
  gap: 6px;
  justify-content: center;
  align-items: center;
}

.ux-star-input {
  position: absolute;
  opacity: 0;
  pointer-events: none;
}

.ux-star-btn {
  padding: 4px;
  background: transparent;
  border: 0;
  line-height: 0;
  cursor: pointer;
  display: inline-flex;
  border-radius: 6px;
}

.ux-star {
  width: 34px;
  height: 34px;
  stroke: currentColor;
  stroke-width: 2;
  fill: none;
  color: #d1d5db;
  transition: transform 150ms ease-in-out, color 150ms ease-in-out;
}

.ux-star-btn:hover .ux-star {
  transform: scale(1.08);
  color: #fde68a;
}

/* selected stars: we don't rely on sibling order; code-behind can set a class if desired.
   Minimal: use focus ring + keep hover. If you want "fill all <= rating", add a class per label from base. */
.ux-star-input:focus-visible + .ux-star-btn {
  outline: 3px solid rgba(13,110,253,.35);
  outline-offset: 3px;
}

.ux-hint {
  margin-top: 8px;
  text-align: center;
  font-size: .9rem;
  color: rgba(0,0,0,.6);
  min-height: 1.2rem;
}

.ux-field {
  margin-top: 12px;
}

.ux-label {
  display: block;
  font-size: .85rem;
  font-weight: 600;
  margin-bottom: 6px;
}

.ux-textarea {
  resize: none;
}

.ux-count {
  margin-top: 4px;
  text-align: right;
  font-size: .8rem;
  color: rgba(0,0,0,.55);
}

.ux-actions {
  margin-top: 12px;
  display: flex;
  gap: 8px;
  justify-content: flex-end;
}

/* Mobile sizing */
@media (max-width: 480px) {
  .ux-rating-card {
    border-radius: 14px;
    padding: 14px;
  }
  .ux-star {
    width: 30px;
    height: 30px;
  }
}
